name: Validate Code Analyzer

on:
  pull_request:
    branches:
      - Full1
      - Full2
jobs:
  code-analyzer-check:
    runs-on: ubuntu-latest
    steps:
      - name: Check out files
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Required to fetch all branches and commits

      - name: Install Salesforce CLI
        run: npm install -g @salesforce/cli@latest

      - name: Install Salesforce Code Analyzer Plugin
        run: echo 'y' | sf plugins install @salesforce/sfdx-scanner@latest

      - name: Get changed metadata files
        id: changed-files
        run: |
          echo "CHANGED_FILES=$(git diff --name-only --diff-filter=d origin/${{ github.event.pull_request.base.ref }} | grep -E '\.(cls|trigger)$' | tr '\n' ',' | sed 's/,$//')" >> $GITHUB_ENV

      - name: Run Salesforce Code Analyzer on changed files only
        id: run-code-analyzer
        if: env.CHANGED_FILES != ''
        uses: forcedotcom/run-code-analyzer@v1
        with:
          run-command: run
          run-arguments: --normalize-severity --target ${{ env.CHANGED_FILES }} --pmdconfig apex-ruleset.xml --outfile results.html
          results-artifact-name: salesforce-code-analyzer-results

      - name: Check the outputs to determine whether to fail
        if: |
          steps.run-code-analyzer.outputs.exit-code > 0 ||
          steps.run-code-analyzer.outputs.num-sev1-violations > 0 ||
          steps.run-code-analyzer.outputs.num-violations > 0
        run: exit 1
